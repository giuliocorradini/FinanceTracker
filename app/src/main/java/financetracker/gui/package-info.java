/**
 * The GUI package contains all the business behind the graphical user interface for this app.
 * An MVC model is employed, hence the division in three subpackages for model, controller and
 * graphical controls. The views are defined in FXML files, under the "resources" directory.
 */
package financetracker.gui;